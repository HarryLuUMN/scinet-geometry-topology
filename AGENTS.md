# AGENTS.md

## Rules

1. Never add unnecessary comments to the code.

2. After every code modification:
   - run `git add`
   - run `git commit`
- run `git push`
   - write a concise commit message.
